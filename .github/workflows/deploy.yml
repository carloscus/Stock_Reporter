# DEPRECATED: This workflow is no longer used.
# All deployment logic has been integrated into "Generar Reportes de Stock" workflow.
# This file is kept for reference but can be safely deleted.

name: Deploy to GitHub Pages (DEPRECATED)

on:
  # No triggers - this workflow is deprecated
  workflow_dispatch:  # Manual trigger only for emergencies

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated Workflow Notice
        run: |
          echo "⚠️  This workflow is DEPRECATED"
          echo "📋 All deployment logic has been moved to 'Generar Reportes de Stock' workflow"
          echo "🔄 Use the integrated workflow instead"
          echo "🗑️  This file can be safely deleted"
